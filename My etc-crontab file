# Global variables
SHELL=/bin/bash
PATH=/sbin:/bin:/usr/sbin:/usr/bin
MAILTO=root
HOME=/

# For details see man 5 crontab

# Example of job definition:
# .---------------- minute (0 - 59)
# |  .------------- hour (0 - 23)
# |  |  .---------- day of month (1 - 31)
# |  |  |  .------- month (1 - 12) OR jan,feb,mar,apr ...
# |  |  |  |  .---- day of week (0 - 6) (Sunday=0 or 7) OR sun,mon,tue,wed,thu,fri,sat
# |  |  |  |  |
# *  *  *  *  * user-name  command to be executed

#   Mins  Hours  Days   Months  Day of the week  Command
    *     *      *      *       *                /.scripts_for_personal_use/fstrim_all_mounted_partitions.bash
#as always make sure this file is ending with a black line like this one:

#make sure you add the users to the group cron: gpasswd -a user_name cron and log back in for it to take affect and execute other cron daemon specific steps, like for cronie managing:
#/etc/fcron/fcron.deny  and /etc/fcron/fcron.allow: 
#1.if both dont exist then all users will be allowed.
#2.if both exist and you want all users to be allowed then leave .deny empty  and put all users in fcron.allow (because the allowed users are all the users in the cron group minus those not mentioned is .allow minus those mentioned in .deny).
#3.if only .deny exists and .allow dosent then all users that arent mentioned in .deny will be allowed.
#generally, the users allowed 
#the exact semantics of managing /etc/fcron/fcron.deny and /etc/fcron.deny depends on the specific cron daemon but the above steps even though are for cronie, should give an idea to help you read them for other cron daemons in the link below:
#https://wiki.gentoo.org/wiki/Cron
#note that in both fcron.deny and fcron.allow, every line should only contain one username only. I am gonna end this file with a empty line like you always should for crontab to execute properly:

